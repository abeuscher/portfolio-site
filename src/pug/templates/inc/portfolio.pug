.portfolio.grid
  .row.pad
    each site in locals.sections[0].sites
      .col-3-m.col-6-xs
        if site.thumbs
          .thumb(data-bg="/img/" + site.thumbs[0], data-info=JSON.stringify(site))
            h3=site.title
