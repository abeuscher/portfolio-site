.row
  .col-12-s
    h2=section.jobs_title
- var count = 0
while count < section.jobs.length
  .job.row
    - var job = section.jobs[count]
      include ./job-row.pug
    if (section.jobs[count+1])
      - var job = section.jobs[count+1];
      include ./job-row.pug
    - count = count + 2
